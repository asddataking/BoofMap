/** Gamification (points, ranks, achievements, referrals) — off until launch. */
export const GAMIFICATION_ENABLED = false;
/** Community forecasting — independent of general gamification. */
export const FORECAST_PULSE_ENABLED = true;
